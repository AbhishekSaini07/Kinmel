<!DOCTYPE html>
<html>
<head>
    <title>DevDomain Splash Screen</title>
    <style>
        /* Reset default margin and padding for the body */
body {
    margin: 0;
    padding: 0;
    background-color: #f5f5f5; /* Set a background color */
}

/* Center the splash screen content vertically and horizontally */
.splash-screen {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 100vh; /* Full viewport height */
}

/* Style the logo image */
.splash-screen img {
    max-width: 50%; /* Limit the width of the logo */
    max-height: 50%; /* Limit the height of the logo */
    filter: drop-shadow(0px 16px 9px #00000061);
    opacity: 0; /* Initially hide the logo */
    animation: fadeIn 3s ease-in-out forwards; /* Animation for fade-in effect */
    transition: opacity 0.5s; /* Transition for opacity change */
    z-index: 2;
}

/* Loader animation */
.loader {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 4px solid #ffffff;
    border-top: 4px solid #3498db;
    animation: spin 2s linear infinite;
    margin-top: 20px;
}

@keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
}

@keyframes fadeIn {
    from { opacity: 0; } /* Start with opacity 0 */
    to { opacity: 1; } /* End with opacity 1 */
}

    </style>
</head>
<body>
    <div class="splash-screen">
        <img src="/images/Kinmel.png" alt="DevDomain Logo">
    </div>
    <script>
        // Add JavaScript to handle the transition to the next page after a delay
        setTimeout(function(){
            window.location.href = "/login"; // Change 'next_page.html' to your actual next page
        }, 5000); // Change 5000 to the duration of your splash screen animation
    </script>
</body>
</html>
